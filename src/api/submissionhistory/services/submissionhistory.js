'use strict';

/**
 * submissionhistory service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::submissionhistory.submissionhistory');
